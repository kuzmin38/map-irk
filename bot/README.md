# Бот «Помощник сантехника» — УК «Жемчужина» (мессенджер MAX)

Рабочий бот звена сантехников УК «Жемчужина» (Андрей, Константин, Александр). Умеет:

- 🔍 **Поиск дома по адресу** — пишете адрес («Розы Люксембург 118/5»), бот отвечает,
  наш ли это дом, и даёт кнопки-ссылки на 2ГИС и Яндекс.Карты.
- 🚿 **Стояки квартир** — из таблицы «СТОЯКИ квартир»: по запросу «Седова 65а/2 кв 47»
  бот отвечает этажом, номером стояка и всей цепочкой квартир этого стояка
  снизу вверх (соседи сверху/снизу — кого заливает и кто заливает).
  Данные: `bot/data/risers.json`, 14 таблиц / 21 адрес.
- 🏘 **Наши дома по ЖК** — дома группируются по комплексам УК: Квадрум, Академия,
  Квартал, Центральный парк, Жемчужина, Четыре солнца. Привязка дома к ЖК —
  кнопкой «Указать ЖК» в карточке дома.
- 📁 **Документы домов (оцифровка)** — прямо в бота можно прислать фото, скан или
  файл техдокументации, он привяжет её к дому (кнопка «Добавить документ»).
  Файлы хранятся в `bot/data/docs/<id дома>/`, список — в базе.
- 📋 **Заявки** — создание (адрес → описание), статусы «новая → в работе → выполнена»,
  список открытых и выполненных. Хранятся в SQLite (`bot/data/bot.db`).
- 🗂 **Электронные паспорта домов** — техданные по каждому дому (розливы, диаметры,
  запорная арматура, доступ в подвал и т.д.). Заполняются прямо из бота кнопкой
  «Редактировать» и сохраняются в базе.
- 📅 **Работы и дедлайны** — по каждому дому: опрессовка, сдача теплового узла,
  дефектовка, закупка материалов. У работы есть срок, ответственный (из звена),
  статус «план → в работе → сдано» и заметка. Общий список сортируется по срокам,
  просроченные помечаются ⚠️ и поднимаются наверх. Состав звена — `bot/data/team.json`.
- 📖 **Справочник** — телефоны, нормативы (ПП № 354), предельные сроки устранения
  (Правила № 170), шпаргалка по диаметрам труб. Правится в `bot/data/directory.json`.

## 1. Создание бота в MAX

Боты создаются через бизнес-кабинет **business.max.ru** (MasterBot больше
не создаёт ботов сам, а отправляет туда же).

1. Зайдите на business.max.ru → «Создание бота».
2. Заполните: название (например, «Люся»), ник (латиницей, 11–60 символов,
   окончание `_bot`), телефон компании и сайт — поля обязательные.
3. После создания бот уходит на модерацию (обычно до 1–2 рабочих дней);
   токен доступа виден в настройках бота в кабинете.
4. Скопируйте **токен** — он понадобится ниже. Никому его не показывайте.

## 2. Установка

Нужен Python 3.10+.

```bash
git clone https://github.com/kuzmin38/map-irk.git
cd map-irk
pip install -r requirements.txt
```

## 3. Запуск

### Локально / для проверки (long polling)

```bash
export MAX_BOT_TOKEN='ваш_токен'
python -m bot.main
```

### На VPS через вебхук

Вебхуку нужен публичный HTTPS-адрес. Проще всего поставить nginx + certbot
и проксировать на порт бота:

```bash
export MAX_BOT_TOKEN='ваш_токен'
export BOT_MODE=webhook
export WEBHOOK_PORT=8080
export WEBHOOK_URL='https://ваш-домен.ru/'   # бот сам зарегистрирует подписку в MAX
python -m bot.main
```

Пример nginx:

```nginx
server {
    listen 443 ssl;
    server_name ваш-домен.ru;
    # ssl_certificate ... (certbot)
    location / { proxy_pass http://127.0.0.1:8080; }
}
```

### Автозапуск (systemd)

Скопируйте `deploy/santehnik-bot.service` в `/etc/systemd/system/`,
впишите свой токен и путь, затем:

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now santehnik-bot
```

## 4. Обновление данных о домах

Список домов (`bot/data/houses.json`) генерируется из карты `index.html`:

```bash
python3 scripts/extract_houses.py
```

Запускайте после обновления карты — список домов подтянется сам.

## 5. Что можно править без кода

- `bot/data/directory.json` — телефоны, памятки, нормативы (впишите реальные
  номера диспетчерской и аварийки вместо прочерков).
- `bot/data/houses.json` — список домов (лучше через `scripts/extract_houses.py`).
- `bot/data/complexes.json` — список ЖК управляющей компании.
- Паспорта домов, заявки, привязки к ЖК и реестр документов живут в
  `bot/data/bot.db`, сами файлы документов — в `bot/data/docs/`.
  Обе папки стоит регулярно бэкапить.

## 6. ИИ-функции (OpenRouter)

Люся умеет писать связные сводки для руководства через OpenRouter (бесплатная
модель по умолчанию). Для включения задайте при запуске:

```bash
export OPENROUTER_API_KEY='ключ'
export OPENROUTER_MODEL='moonshotai/kimi-k2:free'   # можно не задавать — это значение по умолчанию
```

Без ключа всё остальное работает как обычно, кнопка «🧠 Сводка от Люси»
просто подскажет, что ИИ не подключён.

## 7. Деплой на Railway

Люся живёт как отдельный сервис рядом с другими ботами:

1. Railway → **New → GitHub Repo** → выберите `kuzmin38/map-irk`,
   ветка `main`. Python и `requirements.txt`
   Railway определит сам, стартовая команда берётся из `Procfile`
   (`python -m bot.main`, режим long polling — домен и порты не нужны).
2. **Volume** (обязательно, иначе база сотрётся при redeploy):
   в сервисе → Add Volume → mount path `/data`.
3. **Variables** сервиса:
   - `MAX_BOT_TOKEN` — токен бота из business.max.ru
   - `OPENROUTER_API_KEY` — ключ OpenRouter
   - `BOT_DB=/data/bot.db` — база на томе
   - `BOT_DOCS_DIR=/data/docs` — файлы документов на томе
4. Deploy — в логах должно появиться «Запуск в режиме long polling».

## 8. Мини-приложение MAX

Веб-приложение внутри мессенджера: карта домов, раскладка квартир по стоякам,
проектная документация, справочник.

**Отдаёт его сам бот** — тем же процессом на Railway, отдельный хостинг не нужен.
Страница собирается на лету из `miniapp/template.html` и данных `bot/data`,
а привязка домов к ЖК берётся из рабочей базы, поэтому правка в боте видна
в приложении сразу, без пересборки и коммита.

### Публикация

1. Railway → сервис Люси → **Settings → Networking → Generate Domain**.
   Появится адрес вида `lusya-production.up.railway.app`.
2. Railway → **Variables** → добавить `MINIAPP_PATH` — длинную случайную строку.
   Адрес приложения и есть пропуск: без него сервер отдаёт 404 на всё.

   ```bash
   python3 -c "import secrets; print(secrets.token_urlsafe(24))"
   ```

3. Адрес приложения — `https://<домен>/<MINIAPP_PATH>/`.
   Его же показывает команда `/version` в боте.
4. business.max.ru → бот → вкладка **MiniApps** → указать этот адрес.

Проверка живости — `GET /healthz`, отвечает `ok`. Все остальные пути отдают 404:
ни `/`, ни `/miniapp/`, ни файлы данных наружу не выставлены. Страница помечена
`X-Robots-Tag: noindex`, в поиск не попадёт.

### Переменные

- `PORT` — Railway задаёт сам; без него сервер не поднимается (локально можно задать руками).
- `MINIAPP_PATH` — секретный путь. По умолчанию `miniapp` — угадывается, для боевого сервера обязательно смените.
- `MINIAPP_TTL` — сколько секунд держать собранную страницу в памяти, по умолчанию 60.
- `MINIAPP_HOST` — свой домен, если он не railway'ный (иначе берётся `RAILWAY_PUBLIC_DOMAIN`).

В режиме `BOT_MODE=webhook` порт занят диспетчером вебхуков, приложение с него
не отдаётся. На Railway бот работает в long polling, так что всё совместимо.

### Статическая сборка (запасной путь)

`miniapp/index.html` — та же страница, собранная заранее, для GitHub Pages:

```bash
python3 scripts/build_miniapp.py
```

Нужна, только если приложение раздаётся не ботом. Для приватного репозитория
не подходит: Pages с приватного репозитория требуют платный план, и страница
всё равно остаётся публичной.

Кнопка «🗺 Карта и таблицы» появляется в меню бота, как только MAX отдаёт боту
его username. Из карточки дома кнопка открывает приложение сразу на этом доме
(параметр запуска `house:<id>`).

## 9. Оборудование ТП (манометры)

Карточка дома → **🔧 Оборудование ТП**.

Устроено как «место + прибор»: точка установки (тепловой пункт и место —
«ТП №2, подача отопления») заводится один раз и живёт годами, а манометры
в ней меняются. При замене прежний прибор автоматически уходит в историю.

По каждому манометру: заводской номер, срок поверки, кто и когда установил,
фото прибора и фото паспорта. Поверка подсвечивается: ✅ в порядке,
⚠️ меньше 30 дней, ❌ просрочена.

За 30 дней до истечения поверки Люся сама пишет админу, инженеру, мастерам
и руководителю — список приборов, у которых заканчивается срок.

## 10. Рабочий чат «Обслуживание»

Люся добавлена в общий чат администратором — так MAX отдаёт ей весь поток
сообщений (без прав администратора приходят только сообщения с @-упоминанием).

Что она делает:

- **Молчит.** Отвечает, только когда позвали: «Люся, ...», «..., Люся?»
  или @-упоминанием. На остальные сообщения не реагирует.
- **Тихо ведёт летопись.** Каждое сообщение пишется в `chat_messages`:
  кто, когда, текст, были ли файлы. Адрес дома распознаётся в живой речи
  с падежами («на Байкальской 237 течь») и сообщение цепляется к дому.
- **Помечает аварийное** по словам «течёт», «затопило», «прорвало»,
  «нет воды», «засор» и т.п.

Где смотреть: карточка дома → 🔧 Техника дома → **💬 Из чата** — лента
сообщений по этому дому. В брифинге руководства — сводка за день
(сколько сообщений, сколько по домам, что похоже на аварию).

## 11. Расшифровка голосовых и видеоотчётов

Голосовые и видео из рабочего чата Люся расшифровывает сама: `ffmpeg`
вынимает звуковую дорожку (моно, 16 кГц, mp3), распознавание делает модель
OpenRouter тем же ключом, что и остальной ИИ.

- Расшифровка идёт **в фоне** — чат не подтормаживает.
- По распознанному тексту определяется дом и признак аварийного, поэтому
  видеоотчёт без подписи всё равно попадёт в ленту нужного дома.
- Ограничения: файл до 100 МБ, речь до 15 минут, аудио до 20 МБ.
- Картинку видео не анализируем — в отчётах сантехников главное проговаривается
  голосом, а разбор изображения дороже и менее надёжен.
- **Серии роликов сводятся в один ответ.** Отчёты обычно идут пачкой: первое
  видео — сама проблема, дальше ход работы и результат. Люся ждёт, пока поток
  стихнет (`VIDEO_SERIES_WINDOW`, по умолчанию 7 минут), и отвечает один раз
  пересказом «Проблема: … / Сделано: …», ответом на первое видео серии.
  Продолжения без адреса («перекрыли стояк») наследуют дом из начала отчёта.
- **В чат Люся отвечает только на аварийное** (в речи «течь», «затопило»,
  «прорвало», «засор» и т.п.). Рутинные отчёты уходят в базу молча, чтобы не
  засорять чат. Полные расшифровки всех роликов всегда видны в карточке дома.

Настройки:

- `ffmpeg` ставится на Railway через `railpack.json` (`deploy.aptPackages`).
- `OPENROUTER_AUDIO_MODEL` — модель распознавания, по умолчанию
  `google/gemini-2.5-flash`.

Без `ffmpeg` или без ключа расшифровка молча отключается: сообщение
сохраняется как обычно, просто без текста.
